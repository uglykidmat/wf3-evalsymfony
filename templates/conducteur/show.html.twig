{% extends 'base.html.twig' %}

{% block title %}Conducteur{% endblock %}

{% block body %}
    <h1>Conducteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conducteur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ conducteur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ conducteur.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_conducteur_index') }}">Retour à la liste</a>
    <br/>
    <a href="{{ path('app_conducteur_edit', {'id': conducteur.id}) }}">Modifier</a>
    <br/>
    <br/>

    {{ include('conducteur/_delete_form.html.twig') }}
{% endblock %}
